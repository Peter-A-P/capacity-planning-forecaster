"""The results tables: every number in the README's Result section, and nothing else.

PLAN.md section 1. :mod:`headroom.report.tables` formats measured numbers with their
intervals into Markdown and writes them between the README's report markers, so the
tables are only ever produced by `headroom report` and never edited by hand.
"""
