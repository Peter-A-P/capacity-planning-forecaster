# Private working notes, never committed
CLAUDE.local.md

# Secrets
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/

# Data (loaders fetch and verify; only aggregates are committed, and only where the licence allows)
data/raw/
data/nyc/
data/nhs/

# Backtest outputs, model checkpoints, caches
backtest/out/
lightning_logs/
checkpoints/
.cache/
*.parquet

# Dashboard build artefacts. `dashboard/data/*.json` is committed on purpose: it is the
# published site, it is what `headroom export` produces, and the test that checks the built
# output has to be able to read it in CI.
dashboard/dist/
dashboard/node_modules/

# OS and editors
.DS_Store
Thumbs.db
.vscode/
.idea/
